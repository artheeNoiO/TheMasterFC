// Production schema — copy provider/url to schema.prisma when deploying to Supabase/Render
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Models identical to schema.prisma — run: cp schema.postgresql.prisma schema.prisma before deploy
