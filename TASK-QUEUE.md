# TASK-QUEUE — The Master Football Club

คิวงาน AI Automation · **Baseline:** `v0.9.0-stable`

| บทบาท | ใคร | ค่าใช้จ่าย |
|--------|-----|------------|
| เลขา + กระจาย | Era (Home AI) | Ollama ฟรี |
| เขียน patch | Codey | Qwen3 / OpenRouter |
| ตรวจ PASS/FAIL | Claude Code (Director) | Claude credit |
| infra / สั่งตรง | Cursor | Cursor plan |

สถานะ: `todo` → `in_progress` → `ready_for_review` → `done` (หรือ `changes_requested`)

---

## คิวงาน

_(เพิ่ม: `python hub_automation.py enqueue "Made your club" "คำอธิบายงาน"`)_

## รอ Director ตรวจ

_(Claude Code อ่าน diff + `VERDICT: PASS|FAIL`)_

## ปิดแล้ว
- [x] **TASK-011** — เพิ่มฉากจุดโทษ (penalty kick) ใน 2 ไฟล์ ห้ามลบ/แก้โค้ดอื่นที่ไม่เกี่ยวข้องเด็ดขาด (แก้เฉพาะจุดที่ระบุ เพิ่มเข้าไปเท่านั้น ไม่ลบของเดิม): (1) live-pitch-ambient.js — เพิ่มฟังก์ชัน startPenaltyScene(state, attackSide, spot) และ advancePenalty(s, dt) เลียนแบบ startFreekickScene/advanceFreekick ที่มีอยู่แล้วในไฟล์เดียวกัน (ใช้ search_file หา startFreekickScene ก่อน) ไม่มีกำแพง จุดยิงห่างประตู 11 หน่วย (px=goalPx-fwd*11, py=50) โอกาสเข้า 76% เซฟ 16% ชนเสา 8% เรียก beginAmbientShot กับ shotSide,outcome,counted:false,aimTime:0.4 แล้วเพิ่ม else if (s.setPiece?.type === "penalty") { advancePenalty(s, dt); } เข้าไปใน advanceAmbientPitch (dispatcher เดิม ต่อจาก else if freekick แถวเดิม ห้ามลบเงื่อนไขเดิม) (2) football-manager.jsx — บรรทัดที่มี exactly: const inBox = bs.px > 74 || bs.px < 26; (ค้นด้วย search_file pattern="bs.px > 74" จะเจอ context รอบๆ ที่มีตัวแปร foulSide/awayFouled/fkSide/amb อยู่แล้ว) ให้เพิ่มเงื่อนไข if (inBox) { เรียก startPenaltyScene(amb, fkSide) แทนที่จะปล่อยว่าง/ไม่ทำอะไร } else { เดิม (เตะฟรีคิก) } พร้อมเพิ่ม startPenaltyScene เข้า import list ที่มี startFreekickScene อยู่แล้ว (ต้น import จาก ./live-pitch-ambient.js) — ใช้ patch_file เท่านั้นทั้งสองไฟล์ ห้ามใช้ write_file เด็ดขาด, old_text ต้องสั้นและเจาะจง (แค่บรรทัด/ไม่กี่บรรทัดรอบจุดที่แก้จริง ไม่ใช่ทั้งฟังก์ชัน) ห้ามค้นหาเกิน 6 ครั้งก่อนเขียน · owner:Codey · status:done · added:2026-07-03 17:12
- [x] **TASK-010** — เพิ่มฉากจุดโทษ (penalty kick) ใน 2 ไฟล์ ห้ามลบ/แก้โค้ดอื่นที่ไม่เกี่ยวข้องเด็ดขาด (แก้เฉพาะจุดที่ระบุ เพิ่มเข้าไปเท่านั้น ไม่ลบของเดิม): (1) live-pitch-ambient.js — เพิ่มฟังก์ชัน startPenaltyScene(state, attackSide) และ advancePenalty(s, dt) เลียนแบบ startFreekickScene/advanceFreekick ที่มีอยู่แล้วในไฟล์เดียวกัน (ใช้ search_file หา startFreekickScene ก่อน) ไม่มีกำแพง จุดยิงห่างประตู 11 หน่วย (px=goalPx-fwd*11, py=50) โอกาสเข้า 76% เซฟ 16% ชนเสา 8% เรียก beginAmbientShot กับ shotSide,outcome,counted:false,aimTime:0.4 แล้วเพิ่ม else if (s.setPiece?.type === "penalty") { advancePenalty(s, dt); } เข้าไปใน advanceAmbientPitch (dispatcher เดิม ต่อจาก else if freekick แถวเดิม ห้ามลบเงื่อนไขเดิม) (2) football-manager.jsx — บรรทัดที่มี exactly: const inBox = bs.px > 74 || bs.px < 26; (ค้นด้วย search_file pattern="bs.px > 74" จะเจอ context รอบๆ ที่มีตัวแปร foulSide/awayFouled/fkSide/amb อยู่แล้ว) ให้เพิ่มเงื่อนไข if (inBox) { เรียก startPenaltyScene(amb, fkSide) แทนที่จะปล่อยว่าง/ไม่ทำอะไร } else { เดิม (เตะฟรีคิก) } พร้อมเพิ่ม startPenaltyScene เข้า import list ที่มี startFreekickScene อยู่แล้ว (ต้น import จาก ./live-pitch-ambient.js) — ใช้ patch_file เท่านั้นทั้งสองไฟล์ ห้ามใช้ write_file เด็ดขาด, old_text ต้องสั้นและเจาะจง (แค่บรรทัด/ไม่กี่บรรทัดรอบจุดที่แก้จริง ไม่ใช่ทั้งฟังก์ชัน) ห้ามค้นหาเกิน 6 ครั้งก่อนเขียน · owner:Codey · status:done · added:2026-07-03 17:02
- [x] **TASK-009** — เพิ่มฉากจุดโทษ (penalty kick) ใน live-pitch-ambient.js เลียนแบบ startFreekickScene/advanceFreekick ที่มีอยู่แล้วในไฟล์เดียวกัน (ใช้ search_file หา startFreekickScene ก่อน แล้ว patch_file แก้ — ห้าม write_file ไฟล์นี้เพราะไฟล์ใหญ่เกิน 12000 ตัวอักษร) ไม่มีกำแพง จุดยิงห่างประตู 11 หน่วย (px=goalPx-fwd*11, py=50) โอกาสเข้า 76% เซฟ 16% ชนเสา 8% เรียก beginAmbientShot กับ shotSide,outcome,counted:false,aimTime:0.4 แล้วต่อ dispatcher ใน advanceAmbientPitch ให้เรียก advancePenalty ตอน setPiece.type เท่ากับ penalty และแก้ football-manager.jsx: มี comment // TODO: startPenaltyScene(amb, fkSide); อยู่แล้วที่บรรทัดในบล็อกฟาวล์ (if inBox) ให้แทนที่ comment นั้นด้วยการเรียก startPenaltyScene(amb, fkSide); จริง พร้อมเพิ่ม import startPenaltyScene เข้า import list ที่มี startFreekickScene อยู่แล้ว (ใช้ patch_file ทั้งสองไฟล์ ไม่ใช้ write_file, old_text สั้นที่สุดเท่าที่พอระบุตำแหน่งได้ชัดเจน) · owner:Codey · status:done · added:2026-07-03 16:52
- [x] **TASK-008** — เพิ่มฉากจุดโทษ (penalty kick) ใน live-pitch-ambient.js เลียนแบบ startFreekickScene/advanceFreekick ที่มีอยู่แล้วในไฟล์เดียวกัน (ใช้ search_file หา startFreekickScene ก่อน แล้ว patch_file แก้ — ห้าม write_file ไฟล์นี้เพราะไฟล์ใหญ่เกิน 12000 ตัวอักษร) ไม่มีกำแพง จุดยิงห่างประตู 11 หน่วย (px=goalPx-fwd*11, py=50) โอกาสเข้า 76% เซฟ 16% ชนเสา 8% เรียก beginAmbientShot กับ shotSide,outcome,counted:false,aimTime:0.4 แล้วต่อ dispatcher ใน advanceAmbientPitch ให้เรียก advancePenalty ตอน setPiece.type เท่ากับ penalty และแก้ football-manager.jsx บล็อกฟาวล์ให้เรียก startPenaltyScene แทน startFreekickScene เมื่อบอลอยู่ในกรอบเขตโทษ bs.px มากกว่า 74 หรือ น้อยกว่า 26 พร้อมเพิ่ม import startPenaltyScene เข้า import list ที่มีอยู่แล้ว (ใช้ patch_file ทั้งสองไฟล์ ไม่ใช้ write_file ทำให้ old_text สั้นที่สุดเท่าที่พอระบุตำแหน่งได้ชัดเจน อย่าคัดลอกทั้งฟังก์ชันถ้าไม่จำเป็น) · owner:Codey · status:done · added:2026-07-03 16:43
- [x] **TASK-007** — เพิ่มฉากจุดโทษ (penalty kick) ใน live-pitch-ambient.js เลียนแบบ startFreekickScene/advanceFreekick ที่มีอยู่แล้วในไฟล์เดียวกัน (ใช้ search_file หา startFreekickScene ก่อน แล้ว patch_file แก้ — ห้าม write_file ไฟล์นี้เพราะไฟล์ใหญ่เกิน 12000 ตัวอักษร) ไม่มีกำแพง จุดยิงห่างประตู 11 หน่วย (px=goalPx-fwd*11, py=50) โอกาสเข้า 76% เซฟ 16% ชนเสา 8% เรียก beginAmbientShot กับ shotSide,outcome,counted:false,aimTime:0.4 แล้วต่อ dispatcher ใน advanceAmbientPitch ให้เรียก advancePenalty ตอน setPiece.type เท่ากับ penalty และแก้ football-manager.jsx บล็อกฟาวล์ให้เรียก startPenaltyScene แทน startFreekickScene เมื่อบอลอยู่ในกรอบเขตโทษ bs.px มากกว่า 74 หรือ น้อยกว่า 26 พร้อมเพิ่ม import startPenaltyScene เข้า import list ที่มีอยู่แล้ว (ใช้ patch_file ทั้งสองไฟล์ ไม่ใช้ write_file) · owner:Codey · status:done · added:2026-07-03 16:40
- [x] **TASK-006** — เพิ่มฉากจุดโทษ (penalty kick) ใน live-pitch-ambient.js เลียนแบบ startFreekickScene/advanceFreekick ที่มีอยู่แล้วในไฟล์เดียวกัน (ใช้ search_file หา startFreekickScene ก่อน แล้ว patch_file แก้ — ห้าม write_file ไฟล์นี้เพราะไฟล์ใหญ่เกิน 12000 ตัวอักษร) ไม่มีกำแพง จุดยิงห่างประตู 11 หน่วย (px=goalPx-fwd*11, py=50) โอกาสเข้า 76% เซฟ 16% ชนเสา 8% เรียก beginAmbientShot กับ shotSide,outcome,counted:false,aimTime:0.4 แล้วต่อ dispatcher ใน advanceAmbientPitch ให้เรียก advancePenalty ตอน setPiece.type เท่ากับ penalty และแก้ football-manager.jsx บล็อกฟาวล์ให้เรียก startPenaltyScene แทน startFreekickScene เมื่อบอลอยู่ในกรอบเขตโทษ bs.px มากกว่า 74 หรือ น้อยกว่า 26 พร้อมเพิ่ม import startPenaltyScene เข้า import list ที่มีอยู่แล้ว (ใช้ patch_file ทั้งสองไฟล์) · owner:Codey · status:done · added:2026-07-03 16:38
- [x] **TASK-005** — เพิ่มฉากจุดโทษ (penalty kick) ใน live-pitch-ambient.js เลียนแบบ startFreekickScene/advanceFreekick ที่มีอยู่แล้วในไฟล์เดียวกัน (อ่านโค้ดจริงก่อนด้วย read_file) ไม่มีกำแพง จุดยิงห่างประตู 11 หน่วย (px=goalPx-fwd*11, py=50) โอกาสเข้า 76% เซฟ 16% ชนเสา 8% เรียก beginAmbientShot กับ shotSide,outcome,counted:false,aimTime:0.4 แล้วต่อ dispatcher ใน advanceAmbientPitch ให้เรียก advancePenalty ตอน setPiece.type เท่ากับ penalty และแก้ football-manager.jsx บล็อกฟาวล์ให้เรียก startPenaltyScene แทน startFreekickScene เมื่อบอลอยู่ในกรอบเขตโทษ bs.px มากกว่า 74 หรือ น้อยกว่า 26 พร้อมเพิ่ม import startPenaltyScene เข้า import list ที่มีอยู่แล้ว · owner:Codey · status:done · added:2026-07-03 16:05
- [x] **TASK-004** — เพิ่มฉากจุดโทษ (penalty kick) ใน live-pitch-ambient.js เลียนแบบ startFreekickScene/advanceFreekick ที่มีอยู่แล้วในไฟล์เดียวกัน (อ่านโค้ดจริงก่อนด้วย read_file) ไม่มีกำแพง จุดยิงห่างประตู 11 หน่วย (px=goalPx-fwd*11, py=50) โอกาสเข้า 76% เซฟ 16% ชนเสา 8% เรียก beginAmbientShot กับ shotSide,outcome,counted:false,aimTime:0.4 แล้วต่อ dispatcher ใน advanceAmbientPitch ให้เรียก advancePenalty ตอน setPiece.type เท่ากับ penalty และแก้ football-manager.jsx บล็อกฟาวล์ให้เรียก startPenaltyScene แทน startFreekickScene เมื่อบอลอยู่ในกรอบเขตโทษ bs.px มากกว่า 74 หรือ น้อยกว่า 26 พร้อมเพิ่ม import startPenaltyScene เข้า import list ที่มีอยู่แล้ว · owner:Codey · status:done · added:2026-07-03 15:35

- [x] **BASE-002** — แช่ baseline v0.9.0-stable (เวอร์ชันหลัก + แช่) · owner:Human · status:done
- [x] **BASE-001** — แช่ baseline v0.8.0-stable · owner:Human · status:done
- [x] **TASK-003** — เพิ่มฉากจุดโทษ (penalty) — ทดสอบส่งให้ Codey (hub_automation.py process) แล้ว **ใช้ไม่ได้จริง**: เครื่องมือ `dev_team.py` เป็นตัวสร้างเกม Python console ใหม่ (hardcode เขียนไฟล์ `game.py` เสมอ) ไม่ได้ออกแบบมาแก้ไฟล์โปรเจคที่มีอยู่แล้ว ผลลัพธ์ที่ได้ไม่เกี่ยวข้องกับ brief เลย · owner:Codey · status:changes_requested · 2026-07-03
- [ ] **TASK-004** — จุดโทษยังไม่เสร็จ (ยังไม่เคยแตะ live-pitch-ambient.js เลยจริงๆ ใน 4 รอบที่ลอง) — **แก้ไข:** เข้าใจผิดก่อนหน้านี้ว่า Codey แก้ import/branding (GAME_NAME/GAME_TAGLINE) นอกขอบเขตซ้ำ 3 รอบ แล้ว revert ทิ้งไปทั้ง 3 รอบ ที่จริงนั่นคืองาน rename ที่ user สั่ง **Cursor** ทำคู่ขนานอยู่จริง (game-version.js มี GAME_NAME="The Master Football Club" ของจริง) ไม่ใช่ Codey หลอน — คืนค่าที่ revert ผิดกลับให้ Cursor แล้ว ขอโทษด้วยที่สับสน ส่วนจุดโทษให้ Claude เขียนเองต่อแทน Codey (ข้อจำกัดจริงที่ยังอยู่: Codey/qwen3 ยังไม่เคยทำงาน patch หลายไฟล์จบสักรอบใน 12 iterations) · owner:Human · status:todo · added:2026-07-03 15:40, corrected:2026-07-03 15:55
